# Build
target/
bin/

# IDE
.classpath
.project
.settings/
*.iml
.idea/
.vscode/
.theia/

# OS
.DS_Store
Thumbs.db

# Maven
dependency-reduced-pom.xml
.flattened-pom.xml

# Lokale Recherche / Notizen (nicht ins Repo)
recherche/

# Generierte Doku-Artefakte (aus dem Build, nicht ins Repo)
docs/generated/

# Session-Arbeitsmaterial (Plaene, Review-Kontexte; nicht ins Repo)
.prompts/

# Lokale Compose-Ueberlagerung (pinnt den Daemon dieser Maschine auf ein Image;
# bewusst maschinenlokal, siehe Kopfkommentar der Datei)
docker-compose.override.yml
